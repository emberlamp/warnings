name: Label PRs

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  label-pr:
    runs-on: ubuntu-latest
    steps:
      - name: Add labels based on changed files
        uses: actions/labeler@v5
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          configuration-path: .github/labeler.yml

      - name: Add app label
        run: |
          gh label create "app:warnings" --repo "${{ github.repository }}" --force 2>/dev/null || true
          gh pr edit ${{ github.event.pull_request.number }} --add-label "app:warnings" --repo "${{ github.repository }}"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
